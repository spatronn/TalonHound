# 2026-08-08

## Deep Search IOC List pending UX cleanup

- Problem after Deep Search submit: calm banner was correct, but normal IOC result chrome still showed underneath ("Running in the background…", `0 matching IOCs`, empty table, Page size / Prev / Next / Export) — misleading handoff.
- Fix (frontend only): `isDeepSearchPending` + `shouldShowIocListResultChrome` gate results chrome; banner copy points to Action Center; keep chips + Refine search; no IOC List polling.
- "View in Action Center" now goes to `/action-center?task=<deep_search_id>` with row highlight/scroll.
- Files: `frontend/src/lib/deepSearch.js`, `actionCenter.js`, `main.jsx`, tests, `package.json` test:ioc-search script.
- Verified: focused + full frontend tests (398), vite production build, `git diff --check`.
- Commit+push+deploy: `9263e59` → origin/main; prod `/opt/TalonHound` pull ff-only; frontend rebuild/recreate. Live asset `index-C1B0X9Hi.js`; HTTPS 200.

## General-purpose API Keys + REST /api/v1 IOC management (implemented, not committed)

- Scope-based auth (`published_feeds:read`, `ioc:create`, `ioc:update`) with fixed profiles `published_feed` / `ioc_management`.
- Migration `145_api_key_scopes_and_ioc_management.sql` (renumbered; prod already has `143_published_feeds_filter_mode.sql` + `144_ioc_deep_searches.sql`).
- POST/PATCH `/api/v1/iocs` Bearer-only; OpenAPI at `/api/docs` + `/api/openapi.json`.
- Offline Swagger via local `swagger-ui-dist` (`/api/docs/static/*`); no CDN.
- Shared `apiIocService` wraps `createManualIoc` for MCP reuse later (no MCP implemented).
- Decisions: confidence `low|medium|high`; tags by name resolve-only; duplicate = normalized type+value → 200 existing; PATCH mutable: confidence/classifications/tags/note.
- Sprint-smoke `operationLock.test.js` / `canRestoreTransition` proven identical on baseline `9263e59`.
- Prod pre-check (read-only): 3 published_feed keys (1 active, 2 soft-deleted); no unexpected key_types; no scopes column yet; API source absent; created_origin all NULL.
- Do not commit/push/migrate/deploy until kirvem asks.
