# 2026-08-08

## SECRET-02 Redis password default removed (not committed)

- Removed `dev-insecure-redis` from `docker-compose.yml`, `backend/lib/ensure-redis-password.js`, `integration/ensure-redis-password.js`, `.env.example`, ops docs.
- Compose now uses `${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}` (fail-closed like JWT_SECRET).
- App validate always throws if neither `REDIS_PASSWORD` nor `REDIS_URL`; never assigns a known default. Exported `validateRedisPasswordEnv()` + unit tests (5 pass).
- Redis remains unpublished on host. Left `sast/` evidence untouched.

## Deep Search IOC List pending UX cleanup

- Problem after Deep Search submit: calm banner was correct, but normal IOC result chrome still showed underneath ("Running in the background…", `0 matching IOCs`, empty table, Page size / Prev / Next / Export) — misleading handoff.
- Fix (frontend only): `isDeepSearchPending` + `shouldShowIocListResultChrome` gate results chrome; banner copy points to Action Center; keep chips + Refine search; no IOC List polling.
- "View in Action Center" now goes to `/action-center?task=<deep_search_id>` with row highlight/scroll.
- Files: `frontend/src/lib/deepSearch.js`, `actionCenter.js`, `main.jsx`, tests, `package.json` test:ioc-search script.
- Verified: focused + full frontend tests (398), vite production build, `git diff --check`.
- Commit+push+deploy: `9263e59` → origin/main; prod `/opt/TalonHound` pull ff-only; frontend rebuild/recreate. Live asset `index-C1B0X9Hi.js`; HTTPS 200.

## General-purpose API Keys + REST /api/v1 IOC management (implemented, not committed)

- Scope-based auth (`published_feeds:read`, `ioc:create`, `ioc:update`) with fixed profiles `published_feed` / `ioc_management`.
- Migration `145_api_key_scopes_and_ioc_management.sql` (renumbered; prod already has `143_published_feeds_filter_mode.sql` + `144_ioc_deep_searches.sql`).
- POST/PATCH `/api/v1/iocs` Bearer-only; OpenAPI at `/api/docs` + `/api/openapi.json`.
- Offline Swagger via local `swagger-ui-dist` (`/api/docs/static/*`); no CDN.
- Shared `apiIocService` wraps `createManualIoc` for MCP reuse later (no MCP implemented).
- Decisions: confidence `low|medium|high`; tags by name resolve-only; duplicate = normalized type+value → 200 existing; PATCH mutable: confidence/classifications/tags/note.
- Sprint-smoke `operationLock.test.js` / `canRestoreTransition` proven identical on baseline `9263e59`.
- Prod pre-check (read-only): 3 published_feed keys (1 active, 2 soft-deleted); no unexpected key_types; no scopes column yet; API source absent; created_origin all NULL.
- Deployed `747d030` → origin/main; prod `/opt/TalonHound` ff-only; migrate `145` applied; backend+frontend rebuild/recreate.
- Post-deploy: scopes backfilled on 3 keys; system source `API` id=9; `/api/openapi.json` + `/api/docs` + local swagger CSS/JS via HTTPS 200.

## `/api/docs` TalonHound dark theme (local override CSS)

- Root cause: stock `swagger-ui.css` is light-theme (`scheme-container` `#fff`, base text `#3b4151`, section headers `hsla(0,0%,100%,.8)`). Prior docs only set `body` bg `#0b1220`, so white strip + low-contrast dark text remained.
- Approach: local override stylesheet `backend/assets/api-docs/swagger-ui-talonhound.css` served at `/api/docs/static/talonhound.css` (same-origin, offline); linked after `swagger-ui.css`. No node_modules edits, no CDN.
- Tokens mirrored from app slate UI (`#0b1220` / `#0f172a` / `#020617`, borders `#334155`, text `#e2e8f0`/`#94a3b8`, muted blue accents).
- Files: `backend/assets/api-docs/swagger-ui-talonhound.css` (new), `backend/routes/apiDocs.js`, `backend/routes/apiDocs.test.js`.
- Tests: 7/7 pass (`apiDocs.test.js`). Not committed / not pushed / not deployed.
- Ship: commit `99e1d34` → origin/main; prod `/opt/TalonHound` ff-only; backend rebuild/recreate only.
- Live: HTTPS 200 for `/api/docs`, `/api/docs/static/talonhound.css`, stock swagger CSS/JS, `/api/openapi.json`; HTML references talonhound.css after swagger-ui.css.

## Group B SSRF-01/02 custom threat feed hardening (not committed)

- Shared policy in `backend/lib/customThreatFeedSsrf.js` (`node:net` BlockList + `isIP`; `dns.promises.lookup({ all: true })`).
- `validateFeedUrl` delegates to shared sync policy; `fetchFeedUrl` DNS-resolves every hop, pins via custom `lookup`, manual redirects, no cross-origin Authorization/credentials.
- Deny default: loopback/unspecified/link-local/RFC1918/ULA/IPv4-mapped-to-forbidden; http(s) only; TLS validation kept.
- Tests: `customThreatFeedSsrf.test.js` (mocked DNS + local HTTP only) + expanded `validateFeedUrl` cases. Run: `npm run test:custom-threat-feeds` from backend.

## Groups H/I/J security remediation (not committed)

- **AUTH-06 CONFIRMED?FIXED:** PUT/POST /api/integrations/:key/credentials (+ test) require ADMIN; run-now left for analysts.
- **AUTH-07 CONFIRMED?FIXED:** GET /api/api-keys + /profiles require ADMIN; still no plaintext secrets in list.
- **IDOR-01 CONFIRMED?FIXED:** rtifactOwnership.js authorizes by equested_by_id; list/dedupe/concurrency by id; historical email-only rows not auto-granted. No new migration (146 already exists for auth_version).
- **JWT-05 CONFIRMED?FIXED:** production Secure unconditional; AUTH_COOKIE_SECURE=0 cannot downgrade prod.
- **Path traversal NMR?FIXED:** host tar already blocked `..` escape; added ssertSafeTarMembers before extract + ZipSlip tests in temp dirs.
- .env.example documents INITIAL_ADMIN_PASSWORD (not legacy password: admin).
- Did not rewrite historical `sast/*` evidence; updated `current-head-remediation-matrix.md` only.
