# 2026-08-02

- File Artifact Known Hashes kayıp analizi + düzeltme (A+B; C gerekmedi):
  - Root cause: TF aynı dosyayı md5/sha1/sha256 ayrı IOC+artifact yapıyor; live `attachProviderExactHashSet` `otherArtifactIds.length > 1` iken blocking conflict yazıyordu (backfill ise merge ediyordu).
  - Fix commit `6e60631` → origin/main + prod pull/build (backend, integration-worker, scheduler).
  - Live dual-write artık `needs_merge_with_ids` ile fan-in merge; backfill sonrası open provider conflict resolve.
  - Provider backfill: dry-run `merged_artifacts=6`; live aynı + conflict id 5/6/7 resolved. TF+MB overlap 14/14 complete.
  - IOC A API `known_hashes` artık SHA256+SHA1+MD5; IOC B değişmedi. C (UI fallback) gerekmedi.
  - Nightly: `integration_runs` değil `integration_queue_jobs` (integration_key=`file-artifact`, cron yerel gece 00:00 / 21:00 UTC); son run 2026-08-01 21:00Z başarı. Scheduler sağlıklı.
- Threat Classifications sıralama UX iyileştirmesi (analiz + uygulama):
  - Alan: `threat_classifications.sort_order` (migration 083); unique yok; seed 0/10/20…
  - Yeni admin endpoint: `POST /api/admin/threat-classifications/reorder` (`ordered_ids`, transaction, Unknown sabit, active→inactive, audit `threat_classification.reordered`)
  - Frontend: Order sütunu kaldırıldı; `@dnd-kit` drag handle + keyboard sorting; formdan Display order kaldırıldı
  - Threat classification analyst override model:
  - Effective = (feed − suppress) ∪ analyst adds; feed evidence immutable
  - Migration `137_ioc_threat_classification_overrides` (action add|suppress, soft clear)
  - Details GET exposes feed/analyst/suppressed/effective; PATCH saves desired effective in transaction
  - UI: single checkbox list with Feed/Analyst labels; card shows effective only
  - Edit modal: Feed/Analyst provenance labels removed (names only); commit `5a30cd4` → origin/main + prod frontend rebuild; live `index-BnHY1o-j.js`
- IOC Details Threat Classifications list/detail mismatch fix:
  - Symptom: list showed Malware Download +1 (Malware); details card only Malware Download (ex. URLhaus `…file_2406cf76a7ca34ca.exe`).
  - Root cause: junction empty; legacy `ioc_items.threat_classification=malware` (import map from `malware_download`); feed tag → `malware_download`. List falls back to legacy; details effective bundle did not.
  - Fix: `resolveAnalystAdditionSlugs` legacy fallback in details path; commit `422688a` → origin/main; prod backend rebuild/recreate; verified effective=`malware_download,malware`.
- Add IOC form Tags field:
  - `POST /api/ioc/ip` accepts `tag_ids` (atomic assign via `ensureIocTagAssignment` after create; invalid/disabled tags → 400 before insert)
  - `GET /api/tags` opened to admin+analyst; honors `q` / `limit` / `exclude_ids`
  - UI: Tags optional picker between Threat Actor and Note (Details-style chip + search dropdown)
  - Commit `cae4f70` → origin/main + prod pull/build (frontend+backend); live `index-BMxfbakT.js`
- Threat actor multi-select (IOC Details + Add + search/export):
  - Migration `138_ioc_threat_actors_multi`: junction `ioc_threat_actors` + backfill from `ioc_items.threat_actor_id`; legacy FK kept as primary (first selected)
  - Lib `backend/lib/iocThreatActors.js`; `PATCH /api/ioc/:id/threat-actors` (+ legacy `/threat-actor` accepts ids array or singular)
  - Response adds `threat_actors` / `threat_actor_ids`; keeps `threat_actor_id` / `threat_actor_name` primary
  - `GET /api/threat-actors` for analyst/admin picker (admin CRUD unchanged)
  - Search DSL EXISTS over junction; export enrich joins multi names; manual create accepts `threat_actor_ids`
  - UI: `ThreatActorMultiSelect` / badges like classifications; card title "Threat Actors"
  - Commit `a3e0369` → origin/main + prod pull/build (frontend+backend); migrate 138 applied (1 new); live `index-DCz3NFZx.js`

- Commit+push+deploy button visual language: `fdd3d1d` ? origin/main; prod /opt/TalonHound pull ff-only; frontend rebuild/recreate. Live asset `index-Cg41sWyw.js`.
- Commit+push+deploy universal button shell: `06f9d20` ? origin/main; prod pull ff-only; frontend rebuild/recreate. Live asset `index-Dr3hKTIG.js`. HTTPS 200.
- Commit+push+deploy IOC Sources ESC/ModalOverlay: `0a80077` ? origin/main; prod pull ff-only; frontend rebuild/recreate. Live asset `index-sdAHczLC.js`. HTTPS 200.
- Commit+push+deploy Published Feeds/Feeds ESC: `0d9076a` ? origin/main; prod frontend rebuild. Live `index-BuwdwruM.js`. HTTPS 200.
- Commit+push+deploy CTF Escape/ModalOverlay: `f7b9b06` ? origin/main; prod frontend rebuild. Live `index-wg3V-4sT.js`. HTTPS 200.
- Published Feeds multi IOC types (repo only; no commit/push/deploy):
  - Expand: migration `139` adds `ioc_types JSONB` + backfill + CHECK + GIN + bridge trigger; **keeps** legacy `ioc_type` + index (ioc_type becomes nullable).
  - Contract: migration `140_...sql.disabled` (enable after smoke) drops trigger/function, `idx_published_feeds_ioc_type`, chk, and `ioc_type` column — only after safety check (no null/empty ioc_types).
  - API: `ioc_types: string[]` only; write to `ioc_types` only; read prefers `ioc_types`, fallback `ioc_type`.
  - Safe deploy: **139 → backend → frontend → smoke/regenerate → enable+apply 140**.
  - Tests: feedFormatter / publishedFeeds / publisher / publicFeeds / migration compat — pass; frontend build OK.
