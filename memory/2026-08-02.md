# 2026-08-02

- File Artifact Known Hashes kayıp analizi (read-only; onay öncesi değişiklik yok):
  - IOC A `13f8…360b` (TF-first → MB-later): SHA256 artifact’ta yalnızca SHA256; MB note/evidence’ta MD5+SHA1 var; open conflict `provider_hash_set_maps_to_multiple_artifacts` (id=5). Sebep: ThreatFox MD5/SHA1/SHA256’yı ayrı IOC+artifact olarak yaratıyor; live `attachProviderExactHashSet` diğer artifact sayısı >1 ise merge etmeden conflict yazıyor.
  - IOC B `c522…7817` (MB-first): SHA256+SHA1+MD5 aynı artifact’ta.
  - API: A’da `file_artifact.known_hashes` = sadece SHA256 ama `file_information.md5/sha1` dolu; UI known_hashes doluysa FI fallback kullanmıyor.
  - Scope: TF+MB overlap 14; missing secondary 3 (üçü de bugünkü open conflict). MB-first 7g %100 complete. `integration_runs` içinde henüz `file_artifact_reconciliation` kaydı yok.
  - Nightly `applyProviderHashSet` çoklu artifact’ı merge eder (live path’ten daha toleranslı); hash silme riski düşük; henüz bu üçlüyü düzeltmiş görünmüyor.
- Threat Classifications sıralama UX iyileştirmesi (analiz + uygulama):
  - Alan: `threat_classifications.sort_order` (migration 083); unique yok; seed 0/10/20…
  - Yeni admin endpoint: `POST /api/admin/threat-classifications/reorder` (`ordered_ids`, transaction, Unknown sabit, active→inactive, audit `threat_classification.reordered`)
  - Frontend: Order sütunu kaldırıldı; `@dnd-kit` drag handle + keyboard sorting; formdan Display order kaldırıldı
  - Threat classification analyst override model:
  - Effective = (feed − suppress) ∪ analyst adds; feed evidence immutable
  - Migration `137_ioc_threat_classification_overrides` (action add|suppress, soft clear)
  - Details GET exposes feed/analyst/suppressed/effective; PATCH saves desired effective in transaction
  - UI: single checkbox list with Feed/Analyst labels; card shows effective only
  - Edit modal: Feed/Analyst provenance labels removed (names only); commit `5a30cd4` → origin/main + prod frontend rebuild; live `index-BnHY1o-j.js`

