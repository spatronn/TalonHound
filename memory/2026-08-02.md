# 2026-08-02

- Threat Classifications sıralama UX iyileştirmesi (analiz + uygulama):
  - Alan: `threat_classifications.sort_order` (migration 083); unique yok; seed 0/10/20…
  - Yeni admin endpoint: `POST /api/admin/threat-classifications/reorder` (`ordered_ids`, transaction, Unknown sabit, active→inactive, audit `threat_classification.reordered`)
  - Frontend: Order sütunu kaldırıldı; `@dnd-kit` drag handle + keyboard sorting; formdan Display order kaldırıldı
  - IOC Details Threat Classifications tutarsızlığı:
  - Kök neden: kart `threat_classifications` + `feed_intelligence.classifications` birleştiriyor; Edit modal yalnız analyst setini draft ediyordu → feed-sourced `Malware Download` seçimsiz görünüyordu.
  - Düzeltme: feed-only sınıflar kartta `(Feed)` ile işaretli; modalda “Provided by feeds (read-only)” bölümünde checked/disabled; Save yalnızca analyst slug’larını yazar. Commit/push/deploy yok.
