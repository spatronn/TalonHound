# 2026-07-17

- Fixed IOC Details Audit/History cross-IOC leak: enrichment audits were matched by shared hostname/root domain (`githubusercontent.com` / `raw.githubusercontent.com`). Now primary scope is `subject_ioc_id` (+ exact full observable fallback). Migration `114_audit_logs_subject_ioc.sql`.
- Fixed IOC tag add/remove: `POST/DELETE /api/ioc/:id/tags` never wrote `ioc.tag.added` / `ioc.tag.removed` audits (only catalog `tag.created` existed). Now writes IOC-scoped audits with subject_ioc_id.
- Added IOC-scoped source/integration tag hide+restore (`ioc_source_tag_overrides`, migration 115). Blue feed tags can be hidden per IOC without mutating evidence or affecting other IOCs.
- IOC Suppressions lifecycle fix: Remove→soft-disable confusion. Now Enable/Disable keep row; Delete soft-deletes (`deleted_at`); Edit modal has Status; effective status active/disabled/expired; matching requires active+not deleted+not expired. Migration `116_ioc_suppressions_soft_delete.sql`.
