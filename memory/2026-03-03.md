# 2026-03-03

- User confirmed demo target host IP again: `192.168.1.251`.
- User wants SSH troubleshooting workflow where assistant directly connects/checks when credentials are provided.
- User confirmed GitHub access context for demo project repo only (`spatronn/demo-runbook`), to reuse for ops when needed.
- Reminder: keep SSH host details in `TOOLS.md` and operational progress in daily memory notes.
