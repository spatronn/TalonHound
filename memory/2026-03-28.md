# 2026-03-28

- Kullanıcı, session başında aynı bilgileri tekrar sormamamı istedi.
- Kalıcı olarak kaydedildi:
  - Demo host `192.168.1.251` erişimi var.
  - SSH key doğrulandı: `~/.ssh/demo_vm_ed25519` (host erişimi), `~/.ssh/demo_runbook_deploy` (GitHub erişimi).
  - Kullanıcıya hitap tercihi: "kirvem".
