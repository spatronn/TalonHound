# 2026-07-27

- Removed legacy IOC Details **Source Evidence** UI section (local, no commit): `SourceEvidenceSection` + helpers from `intelligenceTab.jsx`; deleted `groupFileArtifactObservations`; details API no longer projects evidence rows into `sources` (empty array kept for shape); `file_artifact.source_observations` dropped from detail projection. Kept `ioc_feed_source_evidence` / `file_artifact_source_observations` tables, import dual-write, `/api/ioc/observable/sources` (IOC List modal), memberships, canonical hash redirect. Frontend tests + build OK (`index-Bfl367kS.js`).
