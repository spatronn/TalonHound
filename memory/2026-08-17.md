# 2026-08-17

## TI capability gap re-score (analysis only)

- Same 16-area 0–5 rubric as 2026-08-13 (max 80 → /100). Current repo after Phases 1–5.
- **56/100 → 76/100** (45/80 → 61/80). No area remains at 0.
- Largest gains: STIX/TAXII 0→4, Bulk 1→4, API 2→4, MISP 0→2.
- Smaller: Search 4→5, Annotations 3→4, Export 3→4, Published Feeds 4→5, Handoff 2→3.
- Unchanged (8 areas): Ingestion 4, Lifecycle 4, Enrichment 3, Hash 3, Provenance 4, Freshness 4, Admin 4.
- Verdict shift: daily IOC ops can live in TalonHound; MISP/STIX ingest still external.
- Canvas: `canvases/talonhound-ti-capability-gaps.canvas.tsx` (updated in place).
- No code, migration, commit, push, or deploy.

## IOC bulk selection context leak (deployed)

- Bug: `selectedIds` in `IOCListPage` survived executed query/mode changes, so hidden IOCs from a previous result set could be included in bulk tag/classification/suppress/expire payloads.
- Fix: selection is valid only for one result context (`browse` | `search:<executedQuery>` | `deep:<id>`). Page/cursor/page-size/unexecuted input are excluded. Bulk payload uses `bulkIocIdsForContext`.
- Commit `1bf8d0a` → origin/main. Prod `/opt/TalonHound` ff-only; frontend rebuild/recreate. Live asset `index-DRXaaEkT.js`. Backend untouched, `date_time=ok`.
- Tests: focused bulk+saved 24/24; full frontend 499/499; `vite build` green; `git diff --check` clean.
- Prod UI smoke: browse→search clears; search A→B clears; Clear resets; saved search B clears; same-query browse pagination preserves; bulk tag payload only temp TEST-NET IOCs `3172133–3172136`. Temp user/IOCs/saved searches/source cleaned.

## IOC page-scoped bulk selection (deployed)

- Follow-up: selection still persisted across pagination inside the same query. Context key now includes visible page (browse page number or search/deep cursor) and page size.
- Toolbar copy: `N selected on this page`. Bulk payload intersects with current-page IDs (`bulkIocIdsForVisiblePage`). Query-wide select-all was not added.
- Commit `a11af00` → origin/main. Prod ff-only; frontend rebuild/recreate. Live asset `index-BH6A5U0G.js`. Backend untouched.
- Tests: focused bulk+saved 31/31; full frontend 506/506; `vite build` green; `git diff --check` clean.
- Prod UI smoke: browse next clears; search next clears; header select-all 25 then next → 0; page size 25→50 clears; search change clears; bulk tag payload only `3172143`. Temp TEST-NET-3 IOCs/user/source cleaned.

## Query-wide IOC bulk actions

- Gmail stage 2: after page select-all on an executed non-empty search with exact_count > page, offer `Select all X matching IOCs`. Mode `all_matching` binds the executed DSL; no client ID set.
- PAGE mode unchanged (max 100 explicit IDs). Unfiltered browse cannot enter all_matching. Query/context change clears; pagination keeps all_matching.
- Backend: `POST /api/iocs/bulk/query/{tags,classifications,suppress,expire}` + preview. Re-parses DSL, ignores client count, rejects empty/malformed/expensive. Sync ≤500 (expiration batch); async BullMQ + Action Center above that. Migration 158.
- Tests: frontend 524/524; backend bulk 26/26; search/saved/deep green; vite build green.
- Commit `8d63293` → origin/main. Prod ff-only; backend/frontend/`ioc-bulk-query-worker` rebuild; migration 158 applied. Worker `talonhound-ioc-bulk-query-worker-1` Up; backend healthy.
- Prod smoke on 40 TEST-NET IOCs `203.0.113.1–40`, query `tag equals "qw-bulk-smoke-20260817"` only:
  - UI: browse has no query-wide offer; header → `25 selected on this page` + `Select all 40 matching IOCs` → `All 40 matching IOCs selected`; pagination keeps all_matching; Add tag modal shows exact query + count; POST `/api/iocs/bulk/query/tags` with `selection_mode=all_matching` and no `ioc_ids`; query change clears.
  - API: readonly 403; empty/malformed 400; preview `match_count=40`; fake client count ignored (classification still 40); suppress 40/40; expire 40/40; page-mode max 100 unchanged.
  - Cleanup leftover: users/iocs/sources/tags = 0.

## Query-wide exact count for `2,000+`

- Bug: `Number(null) === 0`, so capped search `exact_count: null` was treated as 0. Preview never ran; Gmail offer never appeared.
- Fix: `isFiniteExactMatchCount` rejects null; page select-all on capped results calls `/iocs/bulk/query/preview`; loading `Checking total matches…`; stale preview bound to executed query; QUERY_TOO_EXPENSIVE surfaced; PAGE actions stay usable.
- Commit `ca911d9` → origin/main. Prod ff-only; frontend rebuild/recreate only. Live asset `index-CDhZ7liE.js`. No migration. Backend untouched. `readyz ok`, `date_time ok`.
- Tests: frontend bulk 58/58; full frontend 537/537; `vite build` green; backend bulk 26/26; search 154/154; saved 11/11.
- Prod UI smoke: `tag equals "macos"` showed `2,000+`; header select → preview `{query, selection_mode}` no IDs → `Select all 2,019 matching IOCs` → `All 2,019 matching IOCs selected`; Add tag modal exact query+count, cancelled; query change cleared. Temp user cleaned. No feed IOC mutation.

## IOC List saved-search delete confirmation

- Previous: IOC List Saved Search **Delete** called `DELETE /api/iocs/saved-searches/:id` immediately (no confirm).
- Fix (frontend only): Delete opens shared `ModalOverlay` confirm bound to the id/name captured at click. Cancel/Escape/backdrop close without deleting; confirm uses `th-btn--danger-solid`, disables while in flight (`Deleting…`), keeps the dialog on failure.
- Files: `frontend/src/lib/iocSavedSearches.js` (+ controller/copy/capture helpers), `frontend/src/lib/iocSavedSearches.test.js`, `frontend/src/main.jsx`.
- Backend unchanged (`DELETE /api/iocs/saved-searches/:id` already correct).
- Tests: focused saved-search 16/16; full frontend 551/551; `vite build` green; `git diff --check` clean.
- Ship: commit `78e0a0e` → origin/main. Prod `/opt/TalonHound` ff-only; frontend rebuild/recreate only. Live asset `index-Cz6VGV5V.js`. No migration. Backend untouched. HTTPS 200; `readyz ok`, `date_time ok`.

## MalwareBazaar provenance miss (SHA256 3e1f4124…f573)

- IOC `3f2a9f4b-c3a7-4c24-922a-b39c3dcccbd7` is ThreatFox-only. No MB IOC, membership, evidence, or artifact observation.
- Three separate artifacts (MD5/SHA1/SHA256), all ThreatFox; no merge/tombstone. Expected until a trusted provider hash set (MB/VT) is ingested.
- Root cause: **A+H**. MB `recent.csv` is last 48h. Integration stack had no runs 2026-08-13 15:48 → 2026-08-16 20:59 (~77h, all builtin feeds). Sample MB first_seen `2026-08-14 06:50:55Z` aged out at `2026-08-16 06:50:55Z`; catch-up CSV min first_seen `2026-08-14 20:49:55Z`. Live CSV 2026-08-17: hash absent, window `2026-08-15 11:48`–`2026-08-17 11:27`.
- Not parser/dedup/API/UI: worker logs `fetched=parsed skipped=0`; SHA256 never in logs; current recent.csv 641/641 have MB membership; 42 TF-owned SHA256 later gained MB membership (cross-source path works).
- Blast radius: 88 TF SHA256 in the missed first_seen window have no MB membership (this IOC is one). Do not website-backfill. Prefer hourly datalake / full CSV reprocess for `first_seen ∈ (last_run, resume-48h]`.
- No prod mutate/backfill/deploy. Tests added in `integration/lib/malwarebazaar.test.js` (parser accepts incident row; import has no first_seen high-water).

## MalwareBazaar gap recovery implementation (not deployed)

- 77h outage classified as **host reboot / infrastructure downtime** (`2026-08-16 20:59`), not a scheduler code defect. Retention-gap recovery is the product fix.
- Official historical source: `GET https://bazaar.abuse.ch/export/csv/full/` Auth-Key header, ZIP `full.csv` (~219 MB / ~515 MB uncompressed, 1,122,326 rows). Hourly datalake zips are malware binaries — rejected.
- Incident SHA256 **is in that dump**: `first_seen` `2026-08-14 06:50:55`, matching SHA1/MD5.
- Migration `159_malwarebazaar_coverage_state.sql`. Replay through exported `upsertMalwareBazaarObservable`. CLI dry-run (do not execute prod):
  `docker compose exec integration-worker npm run dry-run:malwarebazaar -- --from=2026-08-13T13:12:00Z --to=2026-08-14T22:49:55Z`
- Tests: `cd integration && npm run test:malwarebazaar` → 41/41. No commit/push/deploy/production backfill.

## MalwareBazaar pre-deploy verification (2026-08-17)

- Tests/migration/dry-run executed on `192.168.1.190`, not the Windows workstation.
- Verification-only fixes: coverage-unavailable overlay (not silent healthy); re-enqueue `recovery_running`; dry-run D vs E unique SHA256; unguarded recovery complete; dry-run is `default_transaction_read_only` and does not take lock 942007.
- Remote focused MB+labels: 51 pass / 0 fail. Backend migrate 10/10; queue 61/61; related 49/49. sprint-smoke 144/145 with baseline `operationLock.test.js` `canRestoreTransition` (present on HEAD, unrelated).
- Migration 159 applied via `migrate.js` on a throwaway Postgres restored from prod 158 schema-only dump (~250ms). Second migrate: 0 new. Unmodified backend+worker images started against that schema. Prod `talonhound` DB was not migrated.
- Production dry-run (no execute): interval rows 784 unique; already MB 85; local missing MB 94; completely absent 605; incident SHA256 found, would add MB provenance, fan-in expected. Pre/post `n_tup_*` unchanged. Auth-Key not in output.
- No commit/push/deploy/backfill. External host-down monitoring still a follow-up (no Zabbix in repo).

## MalwareBazaar deploy (2026-08-17)

- User requested main commit + push + deploy. Execute-mode historical backfill remains a separate approval; this ship is code + migration 159 only.

