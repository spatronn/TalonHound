# 2026-08-13

## Production timezone remediation (config-only)

- **Problem:** `system_settings.active_system_timezone=Europe/Istanbul` but `.env` still had `SYSTEM_TIMEZONE=UTC` / `TZ=UTC` (leftover from 2026-07-26 bootstrap). Backend pool constructed with `-c TimeZone=UTC`, so `CURRENT_DATE` / Enrichment Usage bucketing followed UTC. `/readyz` `date_time=degraded`, `postgres_session_timezone=UTC`, `configuration_consistency=inconsistent`.
- **Not** Enrichment Usage-specific; app-wide pool session mismatch.
- **Fix:** `.env` → `SYSTEM_TIMEZONE=Europe/Istanbul`, `TZ=Europe/Istanbul`. Backup: `.env.bak.pre-timezone-remediation-20260812210644`.
- **Recreated (no deps):** backend, integration-scheduler, integration-worker, ioc-expiration-worker, ioc-search-export-worker, ioc-deep-search-worker, backup-worker.
- **Not touched:** db, redis, frontend, proxy, volumes, migrations, DB timezone columns.
- **Post:** `date_time=ok`, pool session `Europe/Istanbul`, `CURRENT_DATE=2026-08-13`. Pre-existing 6 telemetry rows on `2026-08-12` intact (all written before Istanbul midnight). Smoke write `rdap/other` landed on `2026-08-13`.
- HEAD unchanged: `62cc611`.

## Spamhaus DROP Enrichment Usage (local lookup instrumentation)

- **Root cause:** `62cc611` left Spamhaus DROP uninstrumented because it has no per-request external API call. Provider Breakdown therefore stayed at 0 after real IOC lookups.
- **Fix:** `POST /api/enrichment/spamhaus-drop/ioc/refresh` now calls `recordEnrichmentUsage` once per completed local lookup. listed and not_listed are both success. external/cache/rate-limit/latency stay 0. Technical CIDR-query errors are failure. GET hydrate, dataset sync, disabled, not_applicable, dataset_not_synced, and invalid_ip are not counted.
- **No** migration, schema change, frontend change, or historical rewrite. Provider key `spamhaus_drop`.
- Ship: commit `eb83a79` → origin/main; prod `/opt/TalonHound` ff-only; backend rebuild/recreate only. No migrate. Live `recordSpamhausLookupUsage` in image; backend healthy; `date_time=ok`. Historical usage rows intact; `spamhaus_drop` still 0 until the next real lookup.

## Custom Threat Feeds “Disable feed” button visual consistency

- **Root cause:** Custom Threat Feeds edit modal (`CustomFeedLifecycleFields`) is a separate Feed settings UI from built-in `FeedSettingsModal`. It used inline `color: #fca5a5` for Disable feed (green `#86efac` for Enable). Built-in already used compact secondary `th-btn`.
- **Fix:** Shared `feedActiveToggleButtonClassName()` → `buttonClassName({ size: 'compact' })`. No behavior change. Confirm modal / delete / `ep-btn-danger` / `br-btn-danger` untouched.
- Tests: frontend `npm test` 465 pass; `npm run build` OK. Not committed.

## Custom Threat Feeds Enable/Disable asymmetric styling

- Keep Disable feed as compact secondary (`th-btn th-btn--secondary th-btn--compact`).
- Restore Enable feed previous green inline style (`color: #86efac`, transparent bg). Helper: `feedActiveToggleButtonProps(isActive)`.
- Built-in `FeedSettingsModal` unchanged (compact for both). Confirm/delete/danger styles untouched.
- Ship: commit `a6808c9` → origin/main; prod `/opt/TalonHound` ff-only; frontend rebuild/recreate (+ proxy recreate; backend container restarted from cached image, no migrate). Live asset `index-Mor-Vvs8.js`; HTTPS 200; readyz 200.

## Feed Expiration Policy: remove Last seen TTL

- Product set is now 3 modes: `never` | `fixed_ttl` | `missing_from_feed_ttl` (UI: Expire when removed from source).
- Canonical values unchanged except `last_seen_ttl` is no longer accepted on PATCH.
- Runtime `canonicalExpirationMode()` maps leftover `last_seen_ttl` → `fixed_ttl` (first_seen clock, no re-seen reset).
- Snapshot gate uses existing `integration_feeds.feed_update_mode`; missing-from-feed disabled/rejected on incremental.
- Migration `154_drop_last_seen_ttl_expiration.sql`: convert policies + recompute memberships from `first_seen_in_feed`; tighten CHECK. Expected hit: OTX default from migration 102.
- Verify: `SELECT COUNT(*) FROM threat_feed_expiration_policies WHERE expiration_mode = 'last_seen_ttl';` (after: 0).
- Tests: backend `test:ioc-expiration` 126 pass; `test:migrate` 5 pass; OTX first-seen 5 pass; integration usomImportNoop 23 pass; frontend `npm test` 474 pass; `npm run build` OK.